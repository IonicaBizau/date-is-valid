"use strict";

/**
 * dateIsInvalid
 * Check if a date is invalid or not.
 *
 * @name dateIsInvalid
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function dateIsInvalid (a, b) {
    return a + b;
};